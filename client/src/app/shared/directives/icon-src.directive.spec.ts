import { IconSrcDirective } from './icon-src.directive';

describe('IconSrcDirective', () => {
  it('should create an instance', () => {
    const directive = new IconSrcDirective();
    expect(directive).toBeTruthy();
  });
});
