import { WithPermissionDirective } from './with-permission.directive';

describe('WithPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new WithPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
